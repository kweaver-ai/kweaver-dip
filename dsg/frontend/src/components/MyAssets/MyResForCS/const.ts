import __ from './locale'

export enum ResRangeEnum {
    Apply = 'apply',
    Department = 'department',
}

export enum ResTypeEnum {
    Catalog = 'catalog',
    View = 'view',
    Api = 'api',
    File = 'file',
}
