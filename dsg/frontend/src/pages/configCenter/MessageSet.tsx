import MessageSet from '@/components/MessageSet'
import styles from '../styles.module.less'

function MessageSetPage() {
    return (
        <div className={styles.configCenterWrapper}>
            <MessageSet />
        </div>
    )
}

export default MessageSetPage
