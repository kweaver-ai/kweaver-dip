import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/drawio.svg'

const DrawioColored = (props: any) => <Icon component={comSvg} {...props} />

export default DrawioColored
