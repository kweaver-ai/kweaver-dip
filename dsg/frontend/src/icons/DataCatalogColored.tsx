import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/dataCatalog.svg'

const DataCatalogColored = (props: any) => (
    <Icon component={comSvg} {...props} />
)

export default DataCatalogColored
