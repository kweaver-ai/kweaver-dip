import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/hive.svg'

const HiveColored = (props: any) => <Icon component={comSvg} {...props} />

export default HiveColored
