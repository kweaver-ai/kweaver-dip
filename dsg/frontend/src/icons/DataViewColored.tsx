import Icon from '@ant-design/icons'
import { ReactComponent as svgComponent } from './svg/colored/dataView.svg'

const DataViewColored = (props: any) => (
    <Icon component={svgComponent} {...props} />
)

export default DataViewColored
