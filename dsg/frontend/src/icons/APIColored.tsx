import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/api.svg'

const APIColored = (props: any) => <Icon component={comSvg} {...props} />

export default APIColored
