import Icon from '@ant-design/icons'
import { ReactComponent as postgresql } from './svg/outlined/Postgresql.svg'

const PostgresqlOutlined = (props: any) => (
    <Icon component={postgresql} {...props} />
)

export default PostgresqlOutlined
