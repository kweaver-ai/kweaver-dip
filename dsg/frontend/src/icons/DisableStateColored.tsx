import Icon from '@ant-design/icons'
import { ReactComponent as disableStateSvg } from './svg/colored/disableState.svg'

const DisableStateColored = (props: any) => (
    <Icon component={disableStateSvg} {...props} />
)

export default DisableStateColored
