import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/postgre.svg'

const PostgreColored = (props: any) => <Icon component={comSvg} {...props} />

export default PostgreColored
