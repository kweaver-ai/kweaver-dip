import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/uniqueFlag.svg'

const UniqueFlagColored = (props: any) => <Icon component={comSvg} {...props} />

export default UniqueFlagColored
