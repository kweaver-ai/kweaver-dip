import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/infoChangeFlag.svg'

const InfoChangeFlagColored = (props: any) => (
    <Icon component={comSvg} {...props} />
)

export default InfoChangeFlagColored
