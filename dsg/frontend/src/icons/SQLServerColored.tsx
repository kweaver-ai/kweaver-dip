import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/sqlserver.svg'

const SQLServerColored = (props: any) => <Icon component={comSvg} {...props} />

export default SQLServerColored
