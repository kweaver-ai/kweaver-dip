import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/schema.svg'

const SchemaColored = (props: any) => <Icon component={comSvg} {...props} />

export default SchemaColored
