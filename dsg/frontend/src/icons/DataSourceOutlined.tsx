import Icon from '@ant-design/icons'
import { ReactComponent as dataSource } from './svg/outlined/dataSource.svg'

const DataSourceOutlined = (props: any) => (
    <Icon component={dataSource} {...props} />
)

export default DataSourceOutlined
