import Icon from '@ant-design/icons'
import { ReactComponent as dataSync } from './svg/colored/dataSync.svg'

const DataSyncColored = (props: any) => <Icon component={dataSync} {...props} />

export default DataSyncColored
