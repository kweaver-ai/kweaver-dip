import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/fieldQuantile.svg'

const FieldQuantileColored = (props: any) => (
    <Icon component={comSvg} {...props} />
)

export default FieldQuantileColored
