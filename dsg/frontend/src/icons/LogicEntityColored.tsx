import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/logicEntity.svg'

const LogicEntityColored = (props: any) => (
    <Icon component={comSvg} {...props} />
)

export default LogicEntityColored
