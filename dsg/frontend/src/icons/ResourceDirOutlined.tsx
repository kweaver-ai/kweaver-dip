import Icon from '@ant-design/icons'
import { ReactComponent as resourceDir } from './svg/outlined/resourceDir.svg'

const ResourceDirOutlined = (props: any) => (
    <Icon component={resourceDir} {...props} />
)

export default ResourceDirOutlined
