import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/standard.svg'

const StandardColored = (props: any) => <Icon component={comSvg} {...props} />

export default StandardColored
