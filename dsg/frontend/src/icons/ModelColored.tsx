import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/model.svg'

const ModelColored = (props: any) => <Icon component={comSvg} {...props} />

export default ModelColored
