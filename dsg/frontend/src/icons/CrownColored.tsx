import Icon from '@ant-design/icons'
import { ReactComponent as component } from './svg/colored/crown.svg'

const CrownColored = (props: any) => <Icon component={component} {...props} />

export default CrownColored
