import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/dsForm.svg'

const DSFormColored = (props: any) => <Icon component={comSvg} {...props} />

export default DSFormColored
